# test/fixtures/users.yml

# Leia mais sobre fixtures em https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  email: user_one@example.com
  # Senha é 'password'. Devise precisa do campo encrypted_password nos fixtures.
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

two:
  email: user_two@example.com
  # Senha é 'password'
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>